<%=

class OpenStruct
  def marshal_dump
    self.each_pair do |key, value|
      case value
      when OpenStruct
        self[key] = value.marshal_dump
      end
    end
  end
end

{
	spec: spec.marshal_dump
}.to_json
%>
