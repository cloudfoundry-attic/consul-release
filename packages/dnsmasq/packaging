# abort script on any command that exits with a non zero value
set -e

tar xvf dnsmasq/dnsmasq-2.76.tar.gz

pushd dnsmasq-2.76 > /dev/null
  make install "PREFIX=${BOSH_INSTALL_TARGET}"
popd > /dev/null
